# Request Workflow & Architecture

This document explains how requests flow through the system, which files handle what, and the complete lifecycle of each API call.

---

## 🏗️ System Architecture Overview

```
Client Request
    ↓
[server.js] - Express App + Middleware
    ↓
[routes.js] - Route Handler
    ↓
[validation.js] - Input Validation
    ↓
[auth.js] - Authentication (if required)
    ↓
[db-helpers.js] - Database Operations
    ↓
[db.js] - SQLite Database
    ↓
Response to Client
```

---

## 📁 File Responsibilities

| File | Purpose | Key Functions |
|------|---------|---------------|
| **server.js** | Express app setup, middleware, error handling | HTTP server initialization |
| **routes.js** | API endpoint handlers, business logic | All route handlers |
| **auth.js** | Token hashing & authentication middleware | `hashToken()`, `authenticateToken()` |
| **validation.js** | Request payload validation | `validateOrganization()`, `validateProject()`, etc. |
| **db-helpers.js** | Promisified database operations | `dbRun()`, `dbGet()`, `dbAll()` |
| **db.js** | Database initialization & schema | SQLite setup, schema creation |

---

## 🔄 Complete Request Flow

### Step-by-Step: What Happens When a Request Arrives

```
1. Client sends HTTP request
   ↓
2. [server.js] Express receives request
   ↓
3. [server.js] Morgan logs the request (timing starts)
   ↓
4. [server.js] JSON parser middleware (express.json())
   ↓
5. [routes.js] Request routed to appropriate handler
   ↓
6. [validation.js] Payload validated (if needed)
   ↓
7. [auth.js] Authentication checked (if required)
   ↓
8. [routes.js] Business logic executed
   ↓
9. [db-helpers.js] Database operation (if needed)
   ↓
10. [db.js] SQLite executes query
   ↓
11. [routes.js] Response JSON created
   ↓
12. [server.js] Morgan logs response with timing
   ↓
13. Client receives response
```

---

## 📍 Endpoint Workflows

### 1️⃣ POST /orgs - Create Organization

**Files involved:** `server.js` → `routes.js` → `validation.js` → `db-helpers.js` → `db.js`

```
┌─────────────────────────────────────────────────────────────────┐
│ CLIENT REQUEST                                                  │
│ POST http://localhost:3002/orgs                                 │
│ Content-Type: application/json                                  │
│ { "name": "Acme Corp" }                                         │
└─────────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────────┐
│ [server.js] Line 14-15: Express middleware                      │
│ - app.use(express.json()) → Parses JSON body                   │
│ - app.use(morgan(...)) → Starts timing                         │
└─────────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────────┐
│ [server.js] Line 31: Routes mounted                             │
│ - app.use('/', routes) → Forwards to routes.js                 │
└─────────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────────┐
│ [routes.js] Line 32-33: Route matched                           │
│ - router.post('/orgs', async (req, res) => { ... })           │
│ - metrics.requests_total++                                      │
│ - startTime = Date.now()                                        │
└─────────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────────┐
│ [routes.js] Line 37: Validation                                 │
│ - const errors = validateOrganization(req.body)                │
└─────────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────────┐
│ [validation.js] Line 6-20: Validate input                       │
│ - Check name exists and is string                              │
│ - Check name not empty                                          │
│ - Check name < 255 chars                                        │
│ - Return array of errors                                        │
└─────────────────────────────────────────────────────────────────┘
                            ↓
                    If errors exist?
                    ├─ YES → Return 400 Bad Request
                    └─ NO → Continue
                            ↓
┌─────────────────────────────────────────────────────────────────┐
│ [routes.js] Line 44-45: Generate ID and prepare data            │
│ - const orgId = uuidv4()                                        │
│ - const { name } = req.body                                     │
└─────────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────────┐
│ [routes.js] Line 48: Database operation                         │
│ - await dbRun('INSERT INTO organizations ...')                 │
└─────────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────────┐
│ [db-helpers.js] Line 7-15: Promisify database call              │
│ - return new Promise((resolve, reject) => {                    │
│     db.run(sql, params, function(err) { ... })                 │
│   })                                                            │
└─────────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────────┐
│ [db.js] Line 11: SQLite executes query                          │
│ - INSERT INTO organizations (id, name) VALUES (?, ?)           │
│ - UNIQUE constraint checked                                     │
└─────────────────────────────────────────────────────────────────┘
                            ↓
                    Success or Error?
                    ├─ UNIQUE violation → Catch, return 409 Conflict
                    ├─ Other error → Catch, return 500 Internal Error
                    └─ SUCCESS → Continue
                            ↓
┌─────────────────────────────────────────────────────────────────┐
│ [routes.js] Line 49-56: Success response                        │
│ - metrics.orgs_created++                                        │
│ - duration = Date.now() - startTime                            │
│ - console.log('[ORG_CREATED] ...')                             │
│ - res.status(201).json({ id, name, message })                  │
└─────────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────────┐
│ [server.js] Line 15: Morgan logs response                       │
│ - POST /orgs 201 124 - 1.668 ms                               │
└─────────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────────┐
│ CLIENT RESPONSE                                                 │
│ Status: 201 Created                                             │
│ {                                                               │
│   "id": "uuid-here",                                            │
│   "name": "Acme Corp",                                          │
│   "message": "Organization created successfully"                │
│ }                                                               │
└─────────────────────────────────────────────────────────────────┘
```

---

### 2️⃣ POST /projects - Create Project

**Files involved:** `server.js` → `routes.js` → `validation.js` → `db-helpers.js` → `db.js`

```
┌─────────────────────────────────────────────────────────────────┐
│ CLIENT REQUEST                                                  │
│ POST http://localhost:3002/projects                             │
│ { "org_id": "uuid", "name": "My Project" }                     │
└─────────────────────────────────────────────────────────────────┘
                            ↓
         [Same middleware flow as POST /orgs]
                            ↓
┌─────────────────────────────────────────────────────────────────┐
│ [routes.js] Line 75: Route handler                              │
│ - router.post('/projects', async (req, res) => { ... })       │
└─────────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────────┐
│ [routes.js] Line 79: Validation                                 │
│ - const errors = validateProject(req.body)                     │
└─────────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────────┐
│ [validation.js] Line 22-38: Validate input                      │
│ - Check name exists and valid                                   │
│ - Check org_id exists and is string                            │
│ - Return errors array                                           │
└─────────────────────────────────────────────────────────────────┘
                            ↓
                    If errors exist?
                    ├─ YES → Return 400 Bad Request
                    └─ NO → Continue
                            ↓
┌─────────────────────────────────────────────────────────────────┐
│ [routes.js] Line 89: Verify organization exists                 │
│ - const org = await dbGet('SELECT id FROM organizations ...')  │
└─────────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────────┐
│ [db-helpers.js] Line 17-23: Execute SELECT query                │
│ - db.get(sql, params, callback)                                │
│ - Returns single row or undefined                               │
└─────────────────────────────────────────────────────────────────┘
                            ↓
                    Organization exists?
                    ├─ NO → Return 404 Not Found
                    └─ YES → Continue
                            ↓
┌─────────────────────────────────────────────────────────────────┐
│ [routes.js] Line 96-101: Insert project                         │
│ - const projectId = uuidv4()                                    │
│ - await dbRun('INSERT INTO projects ...')                      │
│ - Same flow as POST /orgs for database insert                  │
└─────────────────────────────────────────────────────────────────┘
                            ↓
                    Success?
                    ├─ UNIQUE violation → 409 (project name exists in org)
                    └─ SUCCESS → 201 Created
                            ↓
┌─────────────────────────────────────────────────────────────────┐
│ CLIENT RESPONSE                                                 │
│ Status: 201 Created                                             │
│ { "id": "uuid", "org_id": "uuid", "name": "My Project" }      │
└─────────────────────────────────────────────────────────────────┘
```

---

### 3️⃣ POST /tokens - Create API Token

**Files involved:** `server.js` → `routes.js` → `validation.js` → `auth.js` → `db-helpers.js` → `db.js`

```
┌─────────────────────────────────────────────────────────────────┐
│ CLIENT REQUEST                                                  │
│ POST http://localhost:3002/tokens                               │
│ { "project_id": "uuid" }                                        │
└─────────────────────────────────────────────────────────────────┘
                            ↓
         [Same middleware flow]
                            ↓
┌─────────────────────────────────────────────────────────────────┐
│ [routes.js] Line 125: Route handler                             │
│ - router.post('/tokens', async (req, res) => { ... })         │
└─────────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────────┐
│ [routes.js] Line 129: Validation                                │
│ - const errors = validateToken(req.body)                       │
└─────────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────────┐
│ [validation.js] Line 40-48: Validate input                      │
│ - Check project_id exists and is string                        │
└─────────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────────┐
│ [routes.js] Line 139: Verify project exists                     │
│ - const project = await dbGet('SELECT id FROM projects ...')   │
└─────────────────────────────────────────────────────────────────┘
                            ↓
                    Project exists?
                    ├─ NO → Return 404 Not Found
                    └─ YES → Continue
                            ↓
┌─────────────────────────────────────────────────────────────────┐
│ [routes.js] Line 146-149: Generate secure token                 │
│ - const tokenId = uuidv4()                                      │
│ - const rawToken = crypto.randomBytes(32).toString('hex')      │
│   ↑ This is 64 hex characters = 256 bits of entropy           │
│ - const tokenHash = await hashToken(rawToken)                  │
└─────────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────────┐
│ [auth.js] Line 11-13: Hash the token with bcrypt                │
│ - return bcrypt.hash(token, SALT_ROUNDS)                       │
│   ↑ SALT_ROUNDS = 10 (security vs performance balance)        │
└─────────────────────────────────────────────────────────────────┘
                            ↓
              bcrypt hashing (takes ~60ms)
                            ↓
┌─────────────────────────────────────────────────────────────────┐
│ [routes.js] Line 151: Store hash in database                    │
│ - await dbRun('INSERT INTO api_tokens ...')                    │
│ - Stores: tokenId, project_id, tokenHash                       │
│ - ⚠️ rawToken is NEVER stored                                  │
└─────────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────────┐
│ [routes.js] Line 153-161: Return raw token (ONLY ONCE!)         │
│ - metrics.tokens_created++                                      │
│ - console.log('[TOKEN_CREATED] ...')                           │
│ - res.status(201).json({                                       │
│     id: tokenId,                                               │
│     project_id,                                                │
│     token: rawToken  ← Client must save this!                 │
│   })                                                            │
└─────────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────────┐
│ CLIENT RESPONSE                                                 │
│ Status: 201 Created                                             │
│ {                                                               │
│   "id": "token-uuid",                                           │
│   "project_id": "project-uuid",                                 │
│   "token": "64-char-hex-string-SAVE-THIS",                     │
│   "message": "Save this token - shown only once!"               │
│ }                                                               │
└─────────────────────────────────────────────────────────────────┘
```

**⚠️ Important:** The raw token is returned only once! After this response, only the hash exists in the database, and the raw token cannot be retrieved.

---

### 4️⃣ POST /ingest - Ingest Test Results (AUTHENTICATED)

**Files involved:** `server.js` → `routes.js` → `auth.js` → `validation.js` → `db-helpers.js` → `db.js`

This is the most complex flow because it requires authentication!

```
┌─────────────────────────────────────────────────────────────────┐
│ CLIENT REQUEST                                                  │
│ POST http://localhost:3002/ingest                               │
│ Authorization: Bearer <64-char-token>                           │
│ {                                                               │
│   "run_id": "build-123",                                        │
│   "status": "passed",                                           │
│   "duration_ms": 5000,                                          │
│   "timestamp": "2026-01-12T10:00:00Z"                          │
│ }                                                               │
└─────────────────────────────────────────────────────────────────┘
                            ↓
         [Same middleware flow]
                            ↓
┌─────────────────────────────────────────────────────────────────┐
│ [routes.js] Line 171: Route with auth middleware                │
│ - router.post('/ingest', authenticateToken, async (req, res))  │
│   ↑ authenticateToken runs BEFORE the route handler           │
└─────────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────────┐
│ [auth.js] Line 23: Authentication middleware starts             │
│ - async function authenticateToken(req, res, next)             │
└─────────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────────┐
│ [auth.js] Line 24-26: Extract Authorization header              │
│ - const authHeader = req.headers['authorization']              │
│ - Check if starts with 'Bearer '                               │
└─────────────────────────────────────────────────────────────────┘
                            ↓
                    Header valid?
                    ├─ NO → Return 401 Unauthorized
                    └─ YES → Continue
                            ↓
┌─────────────────────────────────────────────────────────────────┐
│ [auth.js] Line 33: Extract token                                │
│ - const token = authHeader.substring(7)  // Remove 'Bearer '   │
└─────────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────────┐
│ [auth.js] Line 41: Get all token hashes from database           │
│ - const tokens = await dbAll('SELECT ... FROM api_tokens')    │
└─────────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────────┐
│ [db-helpers.js] Line 25-31: Execute query                       │
│ - db.all(sql, params, callback)                                │
│ - Returns array of all token records                            │
└─────────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────────┐
│ [auth.js] Line 44-51: Iterate through all tokens                │
│ - for (const storedToken of tokens) {                          │
│     const isValid = await verifyToken(token, hash)             │
│     if (isValid) {                                             │
│       // Found matching token!                                 │
│     }                                                           │
│   }                                                             │
│ ⚠️ This is O(n) - bottleneck at scale!                         │
└─────────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────────┐
│ [auth.js] Line 16-18: Verify token against hash                 │
│ - return bcrypt.compare(token, hash)                           │
│   ↑ Constant-time comparison (prevents timing attacks)        │
│   ↑ Takes ~60ms per comparison                                │
└─────────────────────────────────────────────────────────────────┘
                            ↓
                    Match found?
                    ├─ NO → Continue loop, eventually return 401
                    └─ YES → Continue
                            ↓
┌─────────────────────────────────────────────────────────────────┐
│ [auth.js] Line 47-50: Attach auth context to request            │
│ - req.auth = {                                                 │
│     tokenId: storedToken.id,                                   │
│     projectId: storedToken.project_id                          │
│   }                                                             │
│ - return next()  ← Continue to route handler                   │
└─────────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────────┐
│ [routes.js] Line 171: Route handler now executes                │
│ - async (req, res) => { ... }                                  │
│ - req.auth.projectId is now available!                         │
└─────────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────────┐
│ [routes.js] Line 176: Validation                                │
│ - const errors = validateTestRun(req.body)                     │
└─────────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────────┐
│ [validation.js] Line 50-81: Validate test run payload           │
│ - Check run_id (required, non-empty string)                    │
│ - Check status (must be 'passed' or 'failed')                  │
│ - Check duration_ms (non-negative number)                      │
│ - Check timestamp (valid ISO 8601 date)                        │
└─────────────────────────────────────────────────────────────────┘
                            ↓
                    Validation passed?
                    ├─ NO → Return 400 Bad Request with details
                    └─ YES → Continue
                            ↓
┌─────────────────────────────────────────────────────────────────┐
│ [routes.js] Line 184-186: Extract data                          │
│ - const { run_id, status, duration_ms, timestamp } = req.body │
│ - const { projectId } = req.auth  ← From authentication!      │
└─────────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────────┐
│ [routes.js] Line 189-193: Try to insert (idempotency!)          │
│ - await dbRun(                                                 │
│     'INSERT INTO test_runs (project_id, run_id, ...) ...',    │
│     [projectId, run_id, status, duration_ms, timestamp]       │
│   )                                                             │
└─────────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────────┐
│ [db.js] SQLite checks UNIQUE(project_id, run_id) constraint     │
│                                                                 │
│ CREATE TABLE test_runs (                                        │
│   ...                                                           │
│   UNIQUE(project_id, run_id)  ← Enforces idempotency          │
│ );                                                              │
└─────────────────────────────────────────────────────────────────┘
                            ↓
                    Constraint check?
                    ├─ DUPLICATE → UNIQUE constraint fails
                    └─ NEW → Insert succeeds
                            ↓
            ┌───────────────┴───────────────┐
            │                               │
       DUPLICATE                          SUCCESS
            ↓                               ↓
┌─────────────────────────┐   ┌─────────────────────────────┐
│ [routes.js] Line 203:   │   │ [routes.js] Line 194:       │
│ Catch UNIQUE error      │   │ Insert successful           │
│                         │   │                             │
│ - metrics.duplicate++   │   │ - metrics.ingested++        │
│ - Log: DUPLICATE_RUN    │   │ - Log: TEST_RUN_INGESTED   │
│ - Return 200 OK         │   │ - Return 201 Created        │
│   { duplicate: true }   │   │   { message: "success" }    │
└─────────────────────────┘   └─────────────────────────────┘
            │                               │
            └───────────────┬───────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────────┐
│ CLIENT RESPONSE                                                 │
│                                                                 │
│ First attempt:                                                  │
│ Status: 201 Created                                             │
│ { "message": "Test run ingested successfully", ... }           │
│                                                                 │
│ Retry with same run_id:                                         │
│ Status: 200 OK (not 409!)                                       │
│ { "message": "...idempotent", "duplicate": true }              │
└─────────────────────────────────────────────────────────────────┘
```

**Key Points:**
- Authentication happens **before** the route handler
- `req.auth.projectId` ensures data goes to correct project
- UNIQUE constraint prevents duplicates **atomically**
- Duplicates return 200 OK (safe retry!)

---

### 5️⃣ GET /metrics - View Metrics

**Files involved:** `server.js` → `routes.js`

```
┌─────────────────────────────────────────────────────────────────┐
│ CLIENT REQUEST                                                  │
│ GET http://localhost:3002/metrics                               │
└─────────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────────┐
│ [routes.js] Line 23-26: Route handler                           │
│ - router.get('/metrics', (req, res) => {                       │
│     res.json(metrics);                                         │
│   })                                                            │
│                                                                 │
│ - No database query needed                                      │
│ - Returns in-memory metrics object                              │
└─────────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────────┐
│ CLIENT RESPONSE                                                 │
│ Status: 200 OK                                                  │
│ {                                                               │
│   "orgs_created": 5,                                            │
│   "projects_created": 12,                                       │
│   "tokens_created": 8,                                          │
│   "test_runs_ingested": 1523,                                   │
│   "duplicate_runs_rejected": 47,                                │
│   "requests_total": 1598                                        │
│ }                                                               │
└─────────────────────────────────────────────────────────────────┘
```

---

### 6️⃣ GET /health - Health Check

**Files involved:** `server.js` → `routes.js`

```
┌─────────────────────────────────────────────────────────────────┐
│ CLIENT REQUEST                                                  │
│ GET http://localhost:3002/health                                │
└─────────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────────┐
│ [routes.js] Line 225-230: Route handler                         │
│ - router.get('/health', (req, res) => {                        │
│     res.json({                                                 │
│       status: 'healthy',                                       │
│       timestamp: new Date().toISOString()                      │
│     });                                                         │
│   })                                                            │
└─────────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────────┐
│ CLIENT RESPONSE                                                 │
│ Status: 200 OK                                                  │
│ {                                                               │
│   "status": "healthy",                                          │
│   "timestamp": "2026-01-12T10:00:00.000Z"                      │
│ }                                                               │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🔐 Security Flow: Token Lifecycle

### Token Creation Flow

```
1. POST /tokens request arrives
   ↓
2. [routes.js] Generate random token
   crypto.randomBytes(32).toString('hex')
   ↓
3. [auth.js] Hash with bcrypt
   bcrypt.hash(token, 10)  [~60ms]
   ↓
4. [db.js] Store hash in database
   INSERT INTO api_tokens (id, project_id, token_hash)
   ↓
5. [routes.js] Return raw token to client (ONLY TIME!)
   { "token": "64-char-hex" }
```

### Token Verification Flow (Every /ingest request)

```
1. Extract token from Authorization header
   ↓
2. [db-helpers.js] Get all token hashes
   SELECT id, project_id, token_hash FROM api_tokens
   ↓
3. [auth.js] Loop through each hash
   for (const stored of tokens) {
     ↓
4. [auth.js] Compare with bcrypt
     bcrypt.compare(providedToken, stored.token_hash)  [~60ms]
     ↓
     Match found?
     ├─ NO → Try next hash
     └─ YES → Attach projectId to request
   }
   ↓
5. [routes.js] Use req.auth.projectId in query
   INSERT INTO test_runs (project_id, ...)
```

**Security guarantees:**
- ✅ Token never stored in plain text
- ✅ Bcrypt uses salt (different hash each time)
- ✅ Constant-time comparison (no timing attacks)
- ✅ Token scoped to one project (multi-tenancy safe)

---

## 📊 Data Flow Diagram

```
                    ┌──────────────┐
                    │    CLIENT    │
                    └──────┬───────┘
                           │
                           ↓
                    ┌──────────────┐
                    │  server.js   │  ← Express app
                    │              │  ← Middleware (JSON, Morgan)
                    │              │  ← Error handlers
                    └──────┬───────┘
                           │
                           ↓
                    ┌──────────────┐
                    │  routes.js   │  ← Route handlers
                    │              │  ← Business logic
                    │              │  ← Response formatting
                    └──────┬───────┘
                           │
              ┌────────────┼────────────┐
              ↓            ↓            ↓
       ┌──────────┐ ┌───────────┐ ┌──────────┐
       │ auth.js  │ │validation.│ │db-helpers│
       │          │ │    js     │ │   .js    │
       │ Token    │ │           │ │          │
       │ hashing  │ │ Input     │ │ Promise  │
       │ & verify │ │ checking  │ │ wrappers │
       └──────────┘ └───────────┘ └────┬─────┘
                                        │
                                        ↓
                                 ┌──────────────┐
                                 │    db.js     │
                                 │              │
                                 │   SQLite     │
                                 │   Database   │
                                 └──────────────┘
```

---

## 🎯 Key Architectural Decisions

### 1. Why Middleware Pattern?

```javascript
// [server.js]
app.use(express.json());           // Parses JSON automatically
app.use(morgan(...));              // Logs all requests
app.use('/', routes);              // Routes all requests

// [routes.js]
router.post('/ingest', authenticateToken, handler);
                       ↑ Middleware runs before handler
```

**Benefits:**
- ✅ Separation of concerns
- ✅ Reusable authentication
- ✅ Automatic logging for all routes
- ✅ Clean error handling

### 2. Why Promisified Database Calls?

```javascript
// [db-helpers.js] - Wraps callback-based sqlite3
function dbRun(sql, params) {
  return new Promise((resolve, reject) => {
    db.run(sql, params, (err, result) => {
      if (err) reject(err);
      else resolve(result);
    });
  });
}

// [routes.js] - Clean async/await syntax
await dbRun('INSERT INTO ...');
```

**Benefits:**
- ✅ Modern async/await syntax
- ✅ Easier error handling (try/catch)
- ✅ Cleaner code

### 3. Why Separate Validation Module?

```javascript
// [validation.js] - Reusable validation functions
function validateTestRun(data) {
  const errors = [];
  // ... validation logic
  return errors;
}

// [routes.js] - Clean route handlers
const errors = validateTestRun(req.body);
if (errors.length > 0) {
  return res.status(400).json({ error: 'Validation Error', details: errors });
}
```

**Benefits:**
- ✅ Reusable validation logic
- ✅ Testable in isolation
- ✅ Consistent error format

---

## 🚀 Performance Considerations

### Where Time is Spent (Per Request)

| Operation | Time | Location |
|-----------|------|----------|
| Network latency | ~1-10ms | Client ↔ Server |
| JSON parsing | <1ms | server.js (express.json) |
| Routing | <1ms | server.js → routes.js |
| Validation | <1ms | validation.js |
| **bcrypt hash/compare** | **~60ms** | **auth.js** |
| SQLite query | 1-5ms | db.js |
| Response formatting | <1ms | routes.js |

**🔴 Bottleneck: bcrypt operations (token creation & verification)**

### Why bcrypt is Slow (Intentionally!)

```javascript
// [auth.js]
const SALT_ROUNDS = 10;  // 2^10 = 1024 iterations

// This INTENTIONALLY takes ~60ms
bcrypt.hash(token, 10);
```

**Purpose:** Makes brute-force attacks impractical
- 1 attempt = 60ms
- 1 million attempts = 60,000 seconds = 16.7 hours

**Trade-off:** Security vs Performance

---

## 📝 Summary

### Request Flow Pattern

```
1. Client sends HTTP request
2. Express middleware (JSON parsing, logging)
3. Route matching
4. Authentication (if required)
5. Validation
6. Business logic
7. Database operation
8. Response formatting
9. Logging
10. Client receives response
```

### File Responsibilities

- **server.js**: App setup, middleware, error handling
- **routes.js**: Endpoint handlers, business logic
- **auth.js**: Token security (hashing, verification)
- **validation.js**: Input validation
- **db-helpers.js**: Database abstraction
- **db.js**: Database initialization

### Key Features

✅ **Middleware pattern** for clean separation
✅ **Async/await** for readable code
✅ **Comprehensive validation** with detailed errors
✅ **Secure authentication** with bcrypt
✅ **Database-level idempotency** with UNIQUE constraints
✅ **Structured logging** for observability

---

**Total request time (typical):**
- Unauthenticated endpoints: 2-5ms
- Authenticated endpoints: 60-70ms (bcrypt dominates)
- Token creation: 65-70ms (bcrypt hashing)

